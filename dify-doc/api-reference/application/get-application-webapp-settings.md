# Get Application WebApp Settings

> Used to get the WebApp settings of the application.

## OpenAPI

````yaml en/openapi_completion.json get /site
paths:
  path: /site
  method: get
  servers:
    - url: '{api_base_url}'
      description: >-
        The base URL for the Completion App API. Replace {api_base_url} with the
        actual API base URL provided for your application (e.g., from
        props.appDetail.api_base_url).
      variables:
        api_base_url:
          type: string
          description: Actual base URL of the API
          default: https://api.dify.ai/v1
  request:
    security:
      - title: ApiKeyAuth
        parameters:
          query: {}
          header:
            Authorization:
              type: http
              scheme: bearer
              description: >-
                API Key authentication. For all API requests, include your API
                Key in the `Authorization` HTTP Header, prefixed with 'Bearer '.
                Example: `Authorization: Bearer {API_KEY}`. **Strongly recommend
                storing your API Key on the server-side, not shared or stored on
                the client-side, to avoid possible API-Key leakage that can lead
                to serious consequences.**
          cookie: {}
    parameters:
      path: {}
      query: {}
      header: {}
      cookie: {}
    body: {}
  response:
    '200':
      application/json:
        schemaArray:
          - type: object
            properties:
              title:
                allOf:
                  - type: string
                    description: WebApp name.
              chat_color_theme:
                allOf:
                  - type: string
                    description: 'Chat color theme, in hex format (e.g., #RRGGBB).'
              chat_color_theme_inverted:
                allOf:
                  - type: boolean
                    description: Whether the chat color theme is inverted.
              icon_type:
                allOf:
                  - type: string
                    enum:
                      - emoji
                      - image
                    description: Icon type.
              icon:
                allOf:
                  - type: string
                    description: >-
                      Icon. If it's `emoji` type, it's an emoji symbol; if it's
                      `image` type, it's an image URL.
              icon_background:
                allOf:
                  - type: string
                    description: 'Background color in hex format (e.g., #RRGGBB).'
              icon_url:
                allOf:
                  - type: string
                    format: url
                    nullable: true
                    description: >-
                      Icon URL (likely refers to image type if `icon` field is
                      just a name/id).
              description:
                allOf:
                  - type: string
                    description: Description.
              copyright:
                allOf:
                  - type: string
                    description: Copyright information.
              privacy_policy:
                allOf:
                  - type: string
                    description: Privacy policy link.
              custom_disclaimer:
                allOf:
                  - type: string
                    description: Custom disclaimer.
              default_language:
                allOf:
                  - type: string
                    description: Default language (e.g., en-US).
              show_workflow_steps:
                allOf:
                  - type: boolean
                    description: Whether to show workflow details.
              use_icon_as_answer_icon:
                allOf:
                  - type: boolean
                    description: Whether to replace 🤖 in chat with the WebApp icon.
            refIdentifier: '#/components/schemas/WebAppSettingsResponse'
        examples:
          success:
            value:
              title: My App
              chat_color_theme: '#ff4a4a'
              chat_color_theme_inverted: false
              icon_type: emoji
              icon: 😄
              icon_background: '#FFEAD5'
              icon_url: null
              description: This is my app.
              copyright: all rights reserved
              privacy_policy: ''
              custom_disclaimer: All generated by AI
              default_language: en-US
              show_workflow_steps: false
              use_icon_as_answer_icon: false
        description: WebApp settings of the application.
  deprecated: false
  type: path
components:
  schemas: {}

````